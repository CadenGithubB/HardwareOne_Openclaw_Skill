# Sensitive credentials
.env
!.env.template
hardwareone.devices.json
!hardwareone.devices.json.template

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Temporary files
*.tmp
/tmp/*

# Deploy bundle (build artifact — rebuilt from the repo, never committed)
*.zip

# Brought-over reference docs — operational/security notes + recovered copies; not for publishing
openclawdocs/

# Copied-in reference only (the agent's notes skill); never publish
notes/

# Claude Code local config
.claude/
